{
    "builds": [{
        "src": "socialapp/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "socialapp/wsgi.py"
        }
    ]
}
